EXPO_PUBLIC_API_URL=http://localhost:4000

# Expo loads .env.production automatically for release builds (assembleRelease, or any build
# where Gradle passes --dev false to the bundler) and falls back to plain .env otherwise — see
# https://docs.expo.dev/guides/environment-variables/. Create your own .env(.production), both are
# gitignored.
